import { QueryClient, QueryFunction } from "@tanstack/react-query";
import { setupReactQueryErrorReporting } from "../utils/errorReporter";

async function throwIfResNotOk(res: Response) {
  if (!res.ok) {
    const text = (await res.text()) || res.statusText;
    throw new Error(`${res.status}: ${text}`);
  }
}

export async function apiRequest(
  method: string,
  url: string,
  data?: unknown | undefined,
): Promise<Response> {
  const res = await fetch(url, {
    method,
    headers: data ? { "Content-Type": "application/json" } : {},
    body: data ? JSON.stringify(data) : undefined,
    credentials: "include",
  });

  await throwIfResNotOk(res);
  return res;
}

type UnauthorizedBehavior = "returnNull" | "throw";
export const getQueryFn: <T>(options: {
  on401: UnauthorizedBehavior;
}) => QueryFunction<T> =
  ({ on401: unauthorizedBehavior }) =>
  async ({ queryKey }) => {
    const res = await fetch(queryKey.join("/") as string, {
      credentials: "include",
    });

    if (unauthorizedBehavior === "returnNull" && res.status === 401) {
      return null;
    }

    await throwIfResNotOk(res);
    return await res.json();
  };

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      queryFn: getQueryFn({ on401: "throw" }),
      refetchInterval: false,
      refetchOnWindowFocus: true, // Ricarica quando finestra torna in focus
      refetchOnReconnect: false,
      staleTime: 30000, // 30 secondi cache per ridurre richieste
      gcTime: 5 * 60 * 1000, // 5 minuti garbage collection
      refetchOnMount: true,
      retry: 1,
    },
    mutations: {
      retry: false,
    },
  },
});

// Setup automatic error reporting for React Query
setupReactQueryErrorReporting(queryClient);
